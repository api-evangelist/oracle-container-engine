naftiko: 1.0.0-alpha2
info:
  label: Oracle Container Engine for Kubernetes (OKE) API — Work Requests
  description: 'Oracle Container Engine for Kubernetes (OKE) API — Work Requests. 2 operations. Lead operation: List work
    requests. Self-contained Naftiko capability covering one Oracle Container Engine business surface.'
  tags:
  - Oracle Container Engine
  - Work Requests
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_CONTAINER_ENGINE_API_KEY: ORACLE_CONTAINER_ENGINE_API_KEY
capability:
  consumes:
  - type: http
    namespace: oracle-container-engine-work-requests
    baseUri: https://containerengine.{region}.oci.oraclecloud.com/20180222
    description: Oracle Container Engine for Kubernetes (OKE) API — Work Requests business capability. Self-contained, no
      shared references.
    resources:
    - name: workRequests
      path: /workRequests
      operations:
      - name: listworkrequests
        method: GET
        description: List work requests
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: resourceId
          in: query
          type: string
    - name: workRequests-workRequestId
      path: /workRequests/{workRequestId}
      operations:
      - name: getworkrequest
        method: GET
        description: Get a work request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workRequestId
          in: path
          type: string
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ORACLE_CONTAINER_ENGINE_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: oracle-container-engine-work-requests-rest
    port: 8080
    description: REST adapter for Oracle Container Engine for Kubernetes (OKE) API — Work Requests. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/workrequests
      name: workrequests
      description: REST surface for workRequests.
      operations:
      - method: GET
        name: listworkrequests
        description: List work requests
        call: oracle-container-engine-work-requests.listworkrequests
        with:
          resourceId: rest.resourceId
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workrequests/{workrequestid}
      name: workrequests-workrequestid
      description: REST surface for workRequests-workRequestId.
      operations:
      - method: GET
        name: getworkrequest
        description: Get a work request
        call: oracle-container-engine-work-requests.getworkrequest
        with:
          workRequestId: rest.workRequestId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: oracle-container-engine-work-requests-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle Container Engine for Kubernetes (OKE) API — Work Requests. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: list-work-requests
      description: List work requests
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: oracle-container-engine-work-requests.listworkrequests
      with:
        resourceId: tools.resourceId
      outputParameters:
      - type: object
        mapping: $.
    - name: get-work-request
      description: Get a work request
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: oracle-container-engine-work-requests.getworkrequest
      with:
        workRequestId: tools.workRequestId
      outputParameters:
      - type: object
        mapping: $.
